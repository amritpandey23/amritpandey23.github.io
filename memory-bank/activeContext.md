# Active Context: Amrit's Blog

## 📅 Current Focus
- Developing and maintaining the Nikola blog.
- Ensuring easy development environment setup via Python virtual environment.
- Documenting the project for AI agents (Memory Bank, README).

## 🔄 Recent Changes
- Created a Python virtual environment (`venv/`).
- Installed `Nikola[extras]` within the virtual environment.
- Added an agent-friendly `README.md` with setup and build instructions.
- Initialized the `memory-bank/` with project context files.

## 🚧 Current Working State
- Site configuration is in `conf.py`.
- Source posts are in `posts/`.
- Ready for building (`nikola build`) and local preview (`nikola serve`).
- GitHub Pages deployment configuration is active (`GITHUB_DEPLOY_BRANCH = "master"`).
